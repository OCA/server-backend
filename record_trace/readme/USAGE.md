A context key `no_record_trace` disables logging for a single operation.

A daily scheduled job purges traces older than `record_trace_retention_days` (default `100` days), configurable in **System Parameters** as record_trace_retention_days key.
